import 'package:flutter/material.dart';

class PaginaBasica extends StatelessWidget {
  const PaginaBasica({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      backgroundColor: Colors.red,
    );
  }
}
